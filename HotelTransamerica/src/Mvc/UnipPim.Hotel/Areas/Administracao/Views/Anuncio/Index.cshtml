@model PaginacaoViewModel<AnuncioViewModel>

@{
    <br />
    ViewData["Title"] = "Lista de Anúncios";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a class="btn btn-success" asp-action="NovoAnuncio">Novo Anúncio</a>
</p>
<vc:summary></vc:summary>
<table class="table">
    <thead>
        <tr>
            <th>
                Nome
            </th>
            <th>
                Ativo
            </th>
            <th>
                Quantidade
            </th>
            <th>
                Valor
            </th>                        
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.List) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ativo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Custo)
            </td>                       
            <td>
                <a class="btn btn-success" asp-action="EditarAnuncio" asp-route-id="@item.Id">Editar</a> |
                <a class="btn btn-secondary" asp-action="DetalhesAnuncio" asp-route-id="@item.Id">Detalhes</a> |
                <a class="btn btn-danger" asp-action="DeleteAnuncio" asp-route-id="@item.Id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
<vc:pagination model-pagination="@Model"></vc:pagination>